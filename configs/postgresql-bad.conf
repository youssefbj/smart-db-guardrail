# Configuration PostgreSQL DANGEREUSE — DÉMO UNIQUEMENT
# → Le guardrail IA BLOQUERA cette configuration (Score < 70/100)

# DANGER : accessible depuis tout Internet
listen_addresses = '*'
port = 5432
max_connections = 1000

# DANGER : algorithme cassé depuis 2004
password_encryption = md5

# DANGER : données voyagent en clair
ssl = off

# DANGER : aucune traçabilité
log_connections = off
log_disconnections = off
log_failed_authentications = off
